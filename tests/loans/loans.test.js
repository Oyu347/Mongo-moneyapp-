'use strict';
const assert=require('assert');global.window=global;require('../../src/features/loans/loans.js');const L=global.MongoLoans;
const d={total:120000,openingRemaining:100000,termMonths:12,rate:12,paymentType:'annuity',payments:[{total:11000,principal:9000,interest:2000,extraPrincipal:1000}]};
assert.strictEqual(L.principalPaid(d),10000);assert.strictEqual(L.totalPaid(d),11000);assert.strictEqual(L.interestPaid(d),2000);assert.strictEqual(L.openingRemaining(d),100000);assert.strictEqual(L.paidAmount(d),10000);assert.strictEqual(L.remaining(d),90000);assert.strictEqual(L.remaining(d,5000),85000);
assert.strictEqual(L.remaining({openingRemaining:100000,payments:[{principal:0,extraPrincipal:0,interest:2500,total:2500}]}),100000,'interest alone must not reduce principal');
const rp=L.normalizeRepayment({id:'p1',accountId:'bank',date:'2026-08-30',total:15000,principal:10000,interest:2000,extraPrincipal:3000},90000,{timestamp:'2026-08-30T00:00:00.000Z'});assert.strictEqual(rp.ok,true);assert.strictEqual(rp.payment.total,15000);assert.strictEqual(rp.payment.principal,10000);assert.strictEqual(rp.payment.interest,2000);assert.strictEqual(rp.payment.extraPrincipal,3000);
const corrected=L.normalizeRepayment({accountId:'bank',date:'2026-08-30',total:99999,principal:10000,interest:2000,extraPrincipal:3000},90000);assert.strictEqual(corrected.ok,true);assert.strictEqual(corrected.payment.total,15000,'mismatched total is normalized to split sum');
assert.strictEqual(L.normalizeRepayment({accountId:'bank',date:'2026-08-30',total:10001,principal:10001,interest:0,extraPrincipal:0},10000).reason,'principal_exceeds_remaining');assert.strictEqual(L.normalizeRepayment({date:'2026-08-30',total:1,principal:1},100).reason,'account_required');assert.strictEqual(L.normalizeRepayment({accountId:'bank',total:1,principal:1},100).reason,'date_required');
const zero=L.buildSchedule(120000,0,12,'annuity',0,0);assert.strictEqual(zero.schedule.length,12);assert.strictEqual(zero.monthly,10000);assert.strictEqual(zero.totalInterest,0);assert(Math.abs(zero.schedule.at(-1).balance)<0.01);
const diff=L.buildSchedule(120000,12,12,'diff',0,0);assert(diff.schedule[0].payment>diff.schedule.at(-1).payment);assert(Math.abs(diff.schedule[0].principal-10000)<0.01);
const ann=L.buildSchedule(120000,12,12,'annuity',0,0);const extra=L.buildSchedule(120000,12,12,'annuity',5000,12);assert(extra.schedule.length<ann.schedule.length||extra.totalInterest<ann.totalInterest);
const next=L.nextSplit({total:120000,termMonths:12,rate:12,paymentType:'diff',payments:[]},120000);assert(Math.abs(next.principal-10000)<0.01);assert(Math.abs(next.interest-1200)<0.01);
console.log('Möngö loans regression tests: PASS');
