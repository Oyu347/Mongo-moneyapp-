'use strict';
const assert=require('assert');global.window=global;require('../../src/features/savings/savings.js');const S=global.MongoSavings;
const goal={id:'g1',saved:1000,target:10000};const transfers=[{purpose:'savings',targetId:'g1',amount:2000},{purpose:'internal',targetId:'g1',amount:9999},{purpose:'savings',targetId:'g2',amount:5000}];const txns=[{type:'income',incomePurpose:'savings_interest',goalId:'g1',amount:500},{type:'expense',goalId:'g1',amount:9000},{type:'income',incomePurpose:'other',goalId:'g1',amount:700}];assert.strictEqual(S.transferContribution(goal,transfers),2000);assert.strictEqual(S.interestContribution(goal,txns),500);assert.strictEqual(S.effectiveSaved(goal,transfers,txns),3500);assert.strictEqual(S.progressPct(goal,transfers,txns),35);
const meta=S.normalizeInterestMetadata({interestMode:'payout',annualInterestRate:'12.5',interestFrequency:'quarterly',interestAccountId:'bank',linkedGoalId:'g1',maturityDate:'2027-01-01'});assert.deepStrictEqual(meta,{interestMode:'payout',annualInterestRate:12.5,interestFrequency:'quarterly',interestAccountId:'bank',linkedGoalId:'g1',maturityDate:'2027-01-01'});assert.strictEqual(S.normalizeInterestMetadata({interestMode:'bad',annualInterestRate:-5,interestFrequency:'bad'}).interestMode,'compound');assert.strictEqual(S.normalizeInterestMetadata({annualInterestRate:-5}).annualInterestRate,0);assert.strictEqual(S.interestDestination({id:'sav',interestMode:'compound',interestAccountId:'bank'},'cash'),'sav');assert.strictEqual(S.interestDestination({id:'sav',interestMode:'maturity'},'cash'),'sav');assert.strictEqual(S.interestDestination({id:'sav',interestMode:'payout',interestAccountId:'bank'},'cash'),'bank');assert.strictEqual(S.interestDestination({id:'sav',interestMode:'payout'},'cash'),'cash');assert.strictEqual(S.interestDestination({id:'sav',interestMode:'none'},'cash'),null);assert.strictEqual(S.linkedGoalId({linkedGoalId:'g1',goalId:'legacy'}),'g1');assert.strictEqual(S.linkedGoalId({goalId:'legacy'}),'legacy');
const jan31=new Date(2026,0,31,12),feb=S.addMonthsSafe(jan31,1);assert.strictEqual(feb.getMonth(),1);assert.strictEqual(feb.getDate(),28);assert.deepStrictEqual(S.calendarMonthsDaysBetween(new Date(2026,0,15,12),new Date(2026,2,20,12)),{months:2,days:5});console.log('Möngö savings regression tests: PASS');
