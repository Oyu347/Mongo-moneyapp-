'use strict';
const assert=require('assert');global.window=global;require('../../src/audit/audit.js');const A=global.MongoAudit;
let f=A.ledgerIdFindings([{id:'txn:1'},{id:'txn:1'}],['txn:1','transfer:2']);assert(f.some(x=>x.code==='duplicate-ledger-id'));assert(f.some(x=>x.code==='missing-ledger-id'&&x.id==='transfer:2'));
f=A.transferFindings([{id:'t1',fromId:'a',toId:'a',amount:10},{id:'t2',fromId:'a',toId:'b',amount:0}]);assert(f.some(x=>x.code==='transfer-same-account'));assert(f.some(x=>x.code==='transfer-nonpositive-amount'));
const ledger=[{id:'income:1',toAccountId:'a',amount:200},{id:'transfer:t',fromAccountId:'a',toAccountId:'b',amount:50}];assert.strictEqual(A.ledgerDeltaForAccount('a',ledger),150);assert.strictEqual(A.ledgerDeltaForAccount('b',ledger),50);
f=A.accountBalanceFindings([{id:'a',openingBalance:100},{id:'b',openingBalance:0}],ledger,id=>id==='a'?250:50);assert.deepStrictEqual(f,[]);f=A.accountBalanceFindings([{id:'a',openingBalance:100}],ledger,()=>249);assert.strictEqual(f[0].code,'account-balance-ledger-mismatch');assert.strictEqual(f[0].expected,250);
f=A.transferConservationFindings([{id:'cash',fromId:'bank',toId:'cash',amount:1000}]);assert.deepStrictEqual(f,[],'Bank to Cash is internally conserved and must not create an audit error');
f=A.accountReferenceFindings([{id:'a'}],[{id:'x',fromAccountId:'a',toAccountId:'missing'}]);assert.deepStrictEqual(f.map(x=>x.code),['ledger-unknown-to-account']);
f=A.loanSplitFindings([{id:'lp',kind:'loan_payment',amount:120,principal:80,interest:20,extraPrincipal:0}]);assert.strictEqual(f[0].code,'loan-payment-split-mismatch');f=A.savingsDoubleCountFindings([{id:'e',type:'expense',purpose:'savings',goalId:'g'}],[{purpose:'savings',targetId:'g',amount:100}]);assert.strictEqual(f[0].code,'savings-transfer-expense-double-count-signal');
const clean=A.run({accounts:[{id:'a',openingBalance:0},{id:'b',openingBalance:0}],ledger:[{id:'transfer:t',kind:'transfer',fromAccountId:'a',toAccountId:'b',amount:100}],expectedLedgerIds:['transfer:t'],transfers:[{id:'t',fromId:'a',toId:'b',amount:100}],txns:[],balanceFor:id=>id==='a'?-100:100});assert.deepStrictEqual(clean,[]);console.log('Möngö audit regression tests: PASS');
